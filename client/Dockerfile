# Placeholder client image until Phase 4 Next.js scaffold (W9).
# Keeps CI / compose --profile client paths valid from Phase 0.
FROM alpine:3.20
WORKDIR /app
RUN echo "Ouroboros client placeholder — replace in Phase 4" > README.txt
CMD ["cat", "README.txt"]
